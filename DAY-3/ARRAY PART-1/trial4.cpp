//reverse an array
//i/p {10,5,7,30} o/p {30,7,5,10}

// for efficient approach time complexity O(n)
                        //space complexity O(1)
//use two pointer naive approach time complexity O(n)
                        //space complexity O(n)

void reverse(intarr[],int n){
    int low=0,high=n-1;
    while(low<high){
        swap(arr[low],arr[high])
        loww++;
        high--;
    }
}
